# Single source of truth for app metadata used by all packaging scripts.
# Version is taken from git tag or core/Cargo.toml at build time.

name = "Desktop Runtime"
bundle_id = "io.desktop-runtime.app"
description = "Native desktop runtime with embedded UI"
manufacturer = "Desktop Runtime"
# Windows UpgradeCode (GUID) for upgrade/downgrade detection
upgrade_code = "B7A1A2D3-E4F5-6789-0ABC-DEF123456789"
